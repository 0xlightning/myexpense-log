---
description: Sync changes to GitHub repository
---

This workflow should be followed after making any significant changes to the project files.

// turbo
1. Stage all changes
   `git add .`

2. Commit changes with a descriptive message
   `git commit -m "Updated project code"`

// turbo
3. Push changes to the main branch
   `git push origin main`
